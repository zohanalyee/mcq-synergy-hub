# NOTE: Lovable hosting does NOT process this file (Netlify convention).
# Kept only for portability if this project is ever self-hosted on Netlify.
# Sitemaps are now real static files at /sitemap.xml and /sitemaps/*.xml,
# generated at build time by scripts/generate-sitemaps.mjs — no proxying needed.

/*    /index.html   200
